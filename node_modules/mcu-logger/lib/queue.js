"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.betterQueue = void 0;
const better_queue_1 = __importDefault(require("better-queue"));
let queue;
exports.betterQueue = {
    queue,
    create(logger) {
        this.queue = new better_queue_1.default((batch, cb) => {
            if (batch.args && batch.loggerMethod) {
                if (batch.level && logger[batch.level]) {
                    logger[batch.level](...batch.args, batch.stack, batch.loggerMethod);
                }
            }
            cb();
        }, {});
        return this.queue;
    },
    push() {
        this.queue.push(...arguments);
    }
};
