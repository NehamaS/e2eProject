import Transport = require('winston-transport');
import { Client } from 'ts-nats';
import { TransportSettings } from './common';
export declare class NATSTransport extends Transport {
    client?: Client;
    settings?: TransportSettings;
    customLog: any;
    constructor(opts: any);
    changeChannel(channel: any): void;
    changeLogLevel(level: any): void;
    log(info: any, callback: any): void;
    close(): void;
}
