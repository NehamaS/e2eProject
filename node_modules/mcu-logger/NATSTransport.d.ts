import Transport = require('winston-transport');
import { NatsConnectionOptions, Client } from 'ts-nats';
interface TransportSettings extends NatsConnectionOptions {
    format: any;
    level: string;
    defaultChannel: string;
    container_id: string;
    container_name: string;
}
export declare class NATSTransport extends Transport {
    client?: Client;
    settings?: TransportSettings;
    customLog: any;
    constructor(opts: any);
    changeChannel(channel: any): void;
    changeLogLevel(level: any): void;
    log(info: any, callback: any): void;
    close(): void;
}
export {};
